import 'package:flutter/material.dart';

class MapsScreen extends StatelessWidget {
  final int? pageIndex;
  const MapsScreen({super.key, this.pageIndex});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
